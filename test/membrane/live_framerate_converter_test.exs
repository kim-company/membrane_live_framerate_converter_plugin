defmodule Membrane.LiveFramerateConverterTest do
  use ExUnit.Case
  doctest Membrane.LiveFramerateConverter

  test "greets the world" do
    assert Membrane.LiveFramerateConverter.hello() == :world
  end
end
